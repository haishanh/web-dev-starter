{
  "scripts": {
    "start": "dev-server -b public",
    "build": "NODE_ENV=production webpack --config webpack.config.js --progress --colors"
  },
  "license": "MIT",
  "devDependencies": {
    "webpack": "^3.8.1"
  }
}
